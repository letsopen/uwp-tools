<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UwpTools.Views.JwtToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="JwtTokenTextBox"
                 Header="JWT Token"
                 PlaceholderText="输入JWT Token"
                 Margin="0,0,0,12"
                 TextChanged="JwtTokenTextBox_TextChanged"/>

        <TextBox x:Name="SecretKeyTextBox"
                 Grid.Row="1"
                 Header="密钥（可选）"
                 PlaceholderText="输入密钥用于验证签名"
                 Margin="0,0,0,12"
                 TextChanged="JwtTokenTextBox_TextChanged"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="解析结果" 
                       Style="{ThemeResource SubtitleTextBlockStyle}"
                       Margin="0,0,0,8"/>

            <TextBox x:Name="ResultTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="200"
                     Margin="0,0,0,12"/>

            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        Spacing="8">
                <Button x:Name="CopyButton"
                        Content="复制"
                        Click="CopyButton_Click"/>
                <Button x:Name="ClearButton"
                        Content="清除"
                        Click="ClearButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page> 
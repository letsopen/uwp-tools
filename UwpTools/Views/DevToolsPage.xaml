<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UwpTools.Views.DevToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:UwpTools.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="UWP 开发工具集" 
                   Style="{ThemeResource TitleLargeTextBlockStyle}"
                   Margin="0,0,0,20"/>

        <ListView x:Name="ToolsList"
                  Grid.Row="1"
                  ItemsSource="{x:Null}"
                  ItemClick="ToolsList_ItemClick"
                  IsItemClickEnabled="True"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:ToolItem">
                    <Grid Margin="10" Padding="15" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind Title}" 
                                   Style="{ThemeResource BodyStrongTextBlockStyle}" 
                                   Grid.Row="0"/>
                        <TextBlock Text="{x:Bind Description}" 
                                   Style="{ThemeResource CaptionTextBlockStyle}" 
                                   Grid.Row="1" 
                                   Margin="0,5,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>